# One sitting, everything unseen — the drive sheet, 2026-09-07

`DRIVE-2026-08-31.md` asked 23 questions and came back with **four adjustments and no answers**.
That was a real result and the map took it seriously — three tickets came out of it, and five more
have closed since. But the debt it recorded never got paid, and four of those five closures added
debts of their own. **This file is that sheet's sections A, B, C and E, re-pointed at the app as it
stands today, plus what the six tickets closed since have never had looked at.**

Nothing in the suite renders a pixel. **1,085 tests** say nothing about anything below.

## Read this before launching

**The order is deliberate.** Last time the sheet led with the study tabs and the sitting never
reached light mode — the fourth time in a row it has escaped. So light mode is **section 1**, and
the sections descend from *never seen* to *seen once*. If the sitting stops early, it stops having
answered the things nobody has ever answered.

**Two preconditions, both cheap — and both already met for the second sitting (2026-09-08):**

1. ~~**Run a fresh simulation before anything else.**~~ **Already satisfied.** The newest stored
   run is `65541123` from **2026-09-08 06:24**, three minutes after HEAD `8bb79e6`, so it is a
   post-phase-10 run on the code under drive. 164 runs are stored now, 163 with step rows and
   **20 with monthly capacity** — the earlier 144 predate v25 and still cannot draw Occupation.
   Make another if you want a clean one, but nothing below is blocked on it.
2. ~~**Back the live database up first.**~~ **Done:**
   `flowmap.sqlite.backup-v30-20260908-202918` is the way back. Section 4 writes to a study.

**Phase 9 landed on 2026-09-07, so the Occupation view has changed shape since this sheet was
first written.** Three things will look wrong and are not:

- **`CLAD09` is now on the grid with capacity and no demand**, and the workcenter count is **18**, not
  17. It is scheduled and unrouted — idle rather than absent, which is the whole of
  [Capacity is clipped to demand — should it follow the workcenter periods instead?](https://github.com/matheus-sancha/FlowMap/issues/19).
  The other 24 workcenters are still missing because they have no schedule in any project:
  *unmodelled*, deliberately.
- **The grid is ragged and much wider** — **36 month columns** (Jan 2025 → Dec 2027) where it used
  to be 15, and `CLAD09`'s row **stops at 2026-12** while every other row runs to 2027-12. Blank is
  not zero: past its schedule, nobody has said.
- **The TOTAL column now reads ≈ 24.8 %**, down from 58.3 %, because 21 months of real capacity
  joined the denominator at zero demand. Per-month figures are untouched. One drag of the slicer
  back to the run's own span restores 58.3 % — **check 16 is the place to confirm that.**

**Every figure in sections 2 and 3 was recomputed for run `65541123` on 2026-09-08**, straight from
`simulation_run_steps` and `simulation_run_workcenter_months` on the app's own §10.3 rule — demand
bucketed by `queueStart`, not `processStart`. The sheet's original numbers (28.5 %, 68.4 %, a 148.5 %
peak) belonged to an earlier run and would have read as defects. This run's grid is **18 workcenters ×
36 months**, 2025-01 → 2027-12, with demand in **15** of them, 2025-10 → 2026-12.

| | |
|---|---|
| TOTAL, all 36 months | 82,072 h / 331,199 h = **24.8 %** |
| TOTAL, the run's own 15 months | 82,072 h / 140,828 h = **58.3 %** |
| worst workcenter-month | **CEU32, 2025-12, 142.8 %** (then 131.6, 128.2, 127.4 — all CEU32) |
| busy-time split | process **93.9 %** · rework **3.5 %** · changeover **2.6 %** |
| TTAT, the one workcenter with units = 2 | 8,461 h / 14,824 h = **57.1 %** — as a single machine it would read 114.2 % |

**Phase 10 landed on 2026-09-07 too, after this sheet was written — and it changes no number
below.** It teaches the engine that a labour-paced workcenter goes faster with a bigger crew, but
**every workcenter type in the live plant still ships machine-paced** (`is_labour_paced = 0`, all
eleven, confirmed against the database before this sitting). So section 2's arithmetic is
untouched: the 148.5 % peak, the ≈ 28.5 % TOTAL and the ≈ 68.4 % it returns to all still stand.
What phase 10 *does* add is a surface nobody has looked at — **section 7**.

## Session header — fill this in

```
build label   0.1.0-2026-09-08a
db.open line  <HH:MM:SS> db.open schema 29 from 29
session start 2026-09-07 <HH:MM:SS>
host          Desktop / windows 10.0 build 26200 / locale <xx_XX>
```

| | |
|---|---|
| branch / HEAD | `v2.0` @ phase 10, `8bb79e6`, tree clean |
| `flutter analyze` | **clean** |
| tests | **1,110 passing** |
| schema, live database | **v30**, 164 runs (20 with monthly capacity), 3 studies, 250 orders |
| live database backed up | `flowmap.sqlite.backup-v30-20260908-202918` |

The project is **VSM 2026 Q1**, occupation thresholds at the v29 defaults **85 / 100**, float at
**0 / 30** days. The studies are **Célula 11B** (60 orders), **Célula 11C** (**130** — the one
section 4 wants), **Célula 11D** (60).

---

## 1 — Light mode

**Four sittings have missed this.** `app.dart` sets no `themeMode`, so the app follows Windows:
*Settings → Personalisation → Colours → Choose your default app mode → Light*. The app repaints
live. Every palette below was validated arithmetically in both brightnesses and **seen in neither**.

1. **The five results tabs in light** — Overview, Plan, Gantt, Occupation, Float. The three most
   likely to be wrong: the **Gantt's eight part colours** (chosen under an all-pairs ΔE search in
   [Eight distinguishable part colours may not exist](https://github.com/matheus-sancha/FlowMap/issues/12)
   and never looked at in light), the **float matrix bands**, and the **occupation grid's amber and
   red**.
2. **The readiness strip** — red, under the app bar. `errorContainer` over `onErrorContainer` is the
   pairing most likely to be wrong in a theme nobody has looked at. If nothing is unready, say so
   rather than leaving it blank.
3. **The occupation bars against their gridlines.** The dotted gridlines and the capacity line were
   drawn for dark; the question is whether they survive a light ground without the bars swallowing
   them.
4. **Two study tabs in light**, at least **Flow** and **Demand** — the VSM canvas and a dense grid
   are different problems.

## 2 — Occupation, as it now is

This surface has been rebuilt three times in eight days and the version on screen has been seen
once, briefly. The `Chart | Grid` switch is **gone**: the chart is now the grid's own header through
`PeriodMatrix.banner` — one column width (**72 px**), one frozen gutter, one scrollbar, a bar
directly above its own row of cells.

5. **The chart and the grid are one scroll.** Drag horizontally: a bar must stay over its own
   column, at every unit and every granularity.
6. **Dotted reads as dotted.** The suite asserts where a tick lands, never how a line looks. Are the
   gridlines legible without competing with the **capacity line**, which is the one line this chart
   exists to show bars breaking?
7. **The hours axis is pinned outside the horizontal scroll** — scroll right and confirm it does not
   slide off the left edge. (This was mechanical, not aesthetic: months are 64 px wide.)
8. **Bar tooltips** carry demand, capacity and the process / rework / changeover split. Hover
   several. Recorded against the split and **independently confirmed on 2026-09-08**: rework is
   **3.4–3.6 % in every one of the fifteen months** — 3.5 % in eleven of them. A constant wearing a
   colour. **Changeover is not** — it runs 0.0 % to 3.5 % and is 0.0 % in the three months at the
   ends, so that band does carry information the other does not. The question this check exists to
   settle is whether a band that is always the same width earns a third of the legend.
9. **The row height measures itself** — 60 pt when any row carries a qualifier, 40 pt when none do.
   Switch **Per workcenter** ↔ **Per line** and watch it change. The per-line labels are
   `Fluxo 11B` over `Célula 11`.
10. **All three units.** `%`, **Hours** (demand *stacked over* capacity, not `733/499`), and **Gap**
    (`-234`). Values are centred. Do the numbers stay legible inside a 72 px column at each?
11. **Structural vs order-level — the distinction the whole ticket turns on.** A **structural**
    filter (studies, cells, lines, type, workcenter) moves demand *and* capacity together. An
    **order-level** one (projects, parts, order numbers) leaves capacity fixed, **dims the
    remainder**, and the % label still counts the grey. That is what tells *"CEU27 is 100 % and 80
    of it is yours"* from *"CEU32 is 147 % and none of it is"*. Can you read the difference off a
    cell?
12. **Non-default thresholds.** *Project settings* → the occupation pair beside the float
    thresholds; change **85 / 100** to **70 / 90** (commits on blur). The bands must move. Then try
    a **red below the amber** — it must be *refused*, not stored.
13. **The TOTAL row and column.** TOTAL is along the bottom and always shown; the TOTAL column is
    frozen beside the labels. **Known and unresolved:** under *Per line*, the TOTAL row can equal a
    line's own row character for character, because line rows overlap on shared workcenters and cannot
    sum. **Filter the view to one line first** — corrected 2026-09-08: on the unfiltered 18-workcenter
    grid no line covers everything in view and the collision is arithmetically impossible, so
    switching to *Per line* alone will never show it. Filter to **Fluxo 11B**, then switch. If the
    identical rows read as a bug rather than as arithmetic, say so — it is fog on the map waiting for
    exactly this.

## 3 — The period slicer and the granularity dropdown

New on 2026-09-07 and **never driven**. The slicer replaced `showDateRangePicker`, which chose
*days* while the button printed only year and month.

14. **The slicer's stops** are the union of capacity months and order months — expect **36** since
    phase 9, up from 15. Drag both ends. The empty range must be **unreachable** — and it is by
    construction: `RangeSlider` keeps start ≤ end, a single stop is one whole month, and the full
    span returns `onChanged(null)`, which is *no filter* rather than a filter matching everything
    (§12.1). **Also drive the two date fields beside the track**, which the sheet predates — they
    came out of the first sitting's finding 2 and the track is meant to follow them. The code's own
    note is the thing to test: 36 stops in 220 pt is about **six pixels a month**, so the slider is
    "a good way to sweep and a poor way to land".
15. **The dropdown re-columns the matrices** — Year / Semester / Quarter / Month. **All four
    confirmed translated in all three ARB files** on 2026-09-08: Month/Quarter/Semester/Year ·
    Mes/Trimestre/Semestre/Año · Mês/Trimestre/Semestre/Ano. Occupation must fold; **the Delivery
    Float must not** (its rows are ranks, so coarsening would pour 250 orders into one column of
    250). **Structurally it cannot** — `_granularity` is state inside `occupation_view.dart` and
    `float_matrix_table.dart` has no such control — so what this check really asks is whether the
    Float tab is *visibly* free of the control, rather than showing one that does nothing.
16. **Coarsening hides overload, and that is the measured cost of the control.** Computed for run
    `65541123` on 2026-09-08, across all four grains:

    | grain | columns | worst cell | cells over 100 % |
    |---|---|---|---|
    | **Month** | 36 | **142.8 %** CEU32, 2025-12 | **39** |
    | Quarter | 12 | 122.6 % CEU32, 2026 Q2 | 7 |
    | Semester | 6 | 119.6 % CEU32, 2026 H1 | 1 |
    | **Year** | 3 | **96.1 %** Coating, 2026 | **0** |

    **At Year the overload does not shrink — it vanishes.** Thirty-nine overloaded workcenter-months
    become zero cells above the line, and the worst thing on the grid is a Coating year at 96.1 %,
    which reads as a plant with room to spare. CEU32 alone is over in **8 of its 12 months** and in
    **none of its 2 years**. The sheet's original figures (148.5 % in a month, 101.8 % in the year)
    were an older run and were *kinder* — that one at least still broke the line at Year.

    So the check has real weight: a planner who opens on Year sees no problem at all. Month is the
    default for exactly this reason. Switch to Year and back, confirm the peak returns, and say
    whether the dropdown makes that trade visible enough to be safe. Month is the default for that reason.
    Switch to Year and back and confirm the peak returns — and say whether the dropdown makes the
    trade visible enough to be safe. **Year is three columns now, not two** (phase 9). Then drag
    the slicer back to the run's own months and confirm the TOTAL column climbs from ≈ 24.8 % to
    ≈ 58.3 %: that is the accepted cost of phase 9 being reversible from the filter bar, and it has
    never been seen.

## 4 — The grids

*Writes to the live study — the backup above is the way back.* The suite covers the drag arithmetic
and its clamps, the sort toggle and the ascending-on-a-new-column rule. **What is left is feel**,
which is [#10](https://github.com/matheus-sancha/FlowMap/issues/10)'s own named limit. Owed since
2026-08-31 and untouched.

**Célula 11C → Demand tab**, 130 orders:

17. **Drag by the row-header number** — the frozen 44 px slot. Nothing inside a cell should be
    draggable, and **text selection inside a cell must still work**.
18. **Drag row 1 to the bottom of all 130.** The one thing reasoning could not settle: how the
    **edge auto-scroll** reads at 130 rows — does it run away, stall, or land where you meant?
19. **Drag it back**, and confirm the sequence numbers renumber consistently in both directions.

Then the sort rule — *a surface sorts unless its row order is itself data*:

20. **Sorts, and should:** the study's **Summary** table (arrives alphabetical by workcenter — sort by
    Occupation descending); on **Simulation results → Overview**, the **queue ranking**, the **share
    of flow** and the **parts table**.
21. **The parts table's swatches must not move.** That swatch *is* the Gantt's legend (§8.6). Press
    a heading, confirm **no part changes colour**, then open the **Gantt** and confirm the legend
    still agrees with the bars. This is the trap #10 did not price. *Note while you are there:* 104
    of 146 stored runs exceed eight parts and 76 have 28, so the top eight carry under half a
    28-part Gantt's bars — colour there groups bars rather than naming a part, by decision.
22. **Does not sort, and must not offer it:** the **Production Plan** (its order is the release
    sequence) and the **Delivery Float** matrix (row *r* means rank *r* in that column). Neither
    heading should respond or show a sort affordance.

## 5 — Navigation, state, and the study side

Sections A and B of the 2026-08-31 sheet, still owed — but two of them are now checks on a **fix**
rather than on an unknown, because [The studies pane reopens on a mode switch](https://github.com/matheus-sancha/FlowMap/issues/18)
swept this neighbourhood and found two defects nobody had reported.

23. **The four study tabs phase 3 never opened.** Stay in **Study** mode and walk **Settings ·
    Capacity · Demand · Summary**. Each must draw something. They are now an `IndexedStack`, so all
    five build on study open — watch for a stall on the first open of a study.
24. **The period control is *hidden*, not greyed**, on Settings, Capacity and Demand, and present on
    **Flow** and **Summary**. That reverses §12.1's old "dimmed rather than hidden" and is
    observable only here. **The strip must not jump** as the control appears and disappears.
25. **State survives a study-tab change** — #18's fix. The **VSM map's zoom and pan** and Demand's
    **Parts | Sequence | MM3** used to reset on every tab change and must not now.
26. **`?study=` across a results-tab change.** Enter the run through a study's own route so the
    location carries `?study=` and the filter arrives pre-set. Change results tab — Overview → Gantt
    → Float — and the filter and the query must both hold. Then **switch to Study mode and back**:
    you must land on the study you left, not on the first one. That second one was a real defect,
    silent, with the right id in the URL throughout.
27. **The studies pane is Study-mode chrome.** It must be **absent in Simulation mode** (its taps
    used to navigate out of it), **present in Project Settings**, and its collapse must survive a
    **restart** — it lives in `window.json` beside `maximized`. Move the window after collapsing it,
    restart, and confirm both stuck.

## 6 — Text, after the sweep

[Which explanation on screen earns its place?](https://github.com/matheus-sancha/FlowMap/issues/15)
answered *"too much explanation and random text"* by applying §12.7b per sentence: the Gantt's help
went **352 → 174** characters and **41 of 592 ARB keys** that rendered nowhere were deleted (551
remain). It owes a drive, and one specific question.

28. **Four `ⓘ` on a five-tab strip.** Overview, Plan, Gantt and Float each carry one on the tab
    label; Occupation carries none. Nobody has seen whether that crowds the strip or reads as
    ordinary. This is the check the ticket named for itself.
29. **The paste hint moved** — it now appears where either Demand grid is *empty*, rather than
    standing as permanent explanation. Empty a grid, or open a study that has one.
30. **Is anything still random?** The four things reported on 2026-08-31 all landed. If the
    complaint has a residue, this is where to name it.

## 7 — Pacing, the phase-10 surface

Landed 2026-09-07, **never driven**, and added to this sheet on 2026-09-08. Every type in the live
plant is machine-paced, so nothing on the results side moves until one is changed here — which
makes this section the only place phase 10 is visible at all.

*Writes to the live plant — the backup above is the way back, and check 34 puts it back by hand.*

**Resources → Workcenter types → edit a type:**

31. **The control is a named choice, not a switch.** *Workcenter Capacity Type* offers **Machine
    Pace** and **Operator Pace** — two peers, so the one nobody picks is still an answer. Confirm
    it reads that way beside the icon grid, and that its `ⓘ` is legible at the length it now runs
    to: it is the longest help string in the app, and #15's sweep is one screen away.
32. **The type list says nothing about pacing.** `workcenter_types_view.dart` passes the flag to
    the editor and renders no subtitle for it, so eleven types look identical in the list and the
    only way to read one is to open it. Say whether that matters — the answer may be nothing.
33. **`scheduleOperatorsHelp` points at a control that no longer exists by that name.** Its text
    still says *turn on "Operators set the pace" under Resources › Workcenter types*, from when
    this was a switch. That label — `workcenterTypeLabourPaced` — is now an **orphan key**: defined
    in all three ARBs, generated, referenced by no widget. Read the help under *Operators per
    shift* on a schedule and confirm it sends you somewhere that isn't there.
34. **Flip Coating to Operator Pace and re-run.** This is the field's own finding from the first
    sitting, closing the loop: *"I added more operators per shift to coating. But it didn't
    increase the capacity."* The committed query predicts **6335h → 2772h**, a factor of **2.29**
    — not 3, because Coating is crewed `3/2/2` and the divisor is the crew *where the work starts*,
    weighted by each shift's own length. Confirm the Occupation view moves for Coating **and for
    nothing else**, then **set it back to Machine Pace** so the plant is as it was.
35. **A stored run keeps its own arithmetic.** The 20 runs that can draw Occupation were all made
    machine-paced. Opening one after check 34 must show the old figures, not the new ones — the
    migration repaced nothing, and a stored run is a record rather than a recomputation.

---

## Findings

### First sitting, 2026-09-07, release build `0.1.0-2026-09-07a`

Reported, in the field's own words:

> - Production Plan/Combined -> Can't scroll down
> - Slicer -> Add next to it a date filter with two selections (with calendar helper) start-end -> Slicer follows
> - Occupation -> Add in the filter "Per Workcenter | Per Line" add "Workcenter Type"
> - In Buttons - Captializer each word
> - Occupation columns header are being cut to "Q4 2..." let's fix this

**The sitting did not walk the sheet.** Five findings arrived instead of thirty
answers — the same shape as 2026-08-31, and the third sitting running to produce
adjustments rather than confirmations. **Light mode is still unseen**, on a sheet
that put it first specifically so it could not escape a fifth time.

Two are defects and were **executed the same session**; three are decisions and
are not this file's to make.

**1 — Production Plan / Combined cannot scroll. Fixed.** The by-study plan is
inside a `SingleChildScrollView`; the combined plan is inside an `Expanded` with
nothing scrolling above it, and it asked `resultTable` for `maxHeight: null` —
which that file documents as meaning *the page I sit in scrolls*. Nothing did, so
the table grew past the pane and **clipped**: 130 orders and no way to reach row
30. Its own comment — *"no cap and filling the pane... as tall as the window"* —
was the intent, and `maxHeight: null` was the wrong lever for it. It now takes
the pane's own constraint, which is the mechanism §12.6 already built: a bounded
pane whose heading holds still and whose two bars pin to its edges.

**5 — `Q4 2...`. Fixed.** A column is a declared width (#16) with a 16 pt sort
arrow reserved beside the label whether or not it is the sorted one. 72 was
chosen for `Aug 2026`; `Q4 2026` and `H2 2026` are a character wider, so they
ellipsised — a heading naming a quarter without saying which year, on a grid that
has spanned three of them since phase 9. Quarter and semester columns are 96 now;
month and year keep 72, because `733/499` needs it and `2026` fits it. The width
is read once and handed to the chart's hover strip, which had been reading the
month constant directly — right at month grain and wrong at every other, the
exact fault #16 exists to prevent.

*Recorded and not done: measuring the heading at runtime*, which is what the row
height does one screen above and would be locale-proof. It makes the column width
depend on the font, and the test font here is far wider than the shipping one, so
every existing width assertion in `period_matrix_test.dart` would become a
different question. This is a visual fix and this map settles those by driving.

**A sixth finding arrived by using the app rather than by walking the sheet.** *"I added more
operators per shift to coating. But it didn't increase the capacity."* It is not a bug —
`working_calendar.dart:361` gates on `count > 0` and §7.5 said so twice — but the **model was
wrong**, and the field is what showed it: a CNC is machine-paced, a spray booth is not.
[Operators scale throughput at labour-paced workcenters, and the model denies it](https://github.com/matheus-sancha/FlowMap/issues/20)
carries it, and it is the one thing that has moved this map's Out of scope line. Phase 10.

**2, 3 and 4 were decisions rather than defects.** All three were settled the same session and
2 and 3 are **built**; 4 is a rule rather than a feature and is applied:

- **A start/end date field with a calendar helper, driving the slicer.** This
  partly reverses #17, which *deleted* `showDateRangePicker` — and deleted it
  because it chose **days** while the button printed only year and month, so
  15 Jan–20 Feb read back as `2026-01 → 2026-02`. A calendar picking days brings
  that back; a calendar picking months is a calendar with its days disabled. The
  question is which grain the field commits to, and what happens to #17's
  guarantee that the empty range is unreachable.
- **"Workcenter Type" as a third grouping.** #9 chose two and retired the pivot
  to get *one figure at one grain instead of two at two*. A type row is
  arithmetically the line row's shape — everyone's demand at the workcenters of that
  type over those workcenters' capacity — so the bands carry over, but it inherits
  the line grouping's named cost: rows overlap where a type spans lines, so they
  do not sum and the TOTAL row can equal one of them.
- **Capitalize each word in buttons.** The trap is that this is an **English**
  convention. Title Case is normal for en-US UI labels; Spanish and Portuguese
  use sentence case, and *"Incluir En Simulación"* reads as a mistake to a native
  reader. Applying it to all three ARB files would make two of them wrong, so the
  question is whether the rule is per-locale — and if it is, it is a rule about
  the three files rather than a find-and-replace.

### Second sitting, 2026-09-08, release build `0.1.0-2026-09-08a`

Reported, in the field's own words:

> - app switched to light mode - but i need a place to choose between light and dark in the app settings.
> - The workcenter capacity type "i" icon, I'm trying to see the tool tip but can't.
> - Also When the workcenter capacity type is set to Machine Pace and I can process 2 orders at the same time, how we this is treated in the occupation graph? Because the capacity hours will be the same but can process multiple orders.

**The sitting reached light mode.** Five sittings in, section 1 was finally opened — and it did not
survive first contact: the finding is not about a colour but about the *cost of looking*. Two
defects, both **executed the same session**; one question, answered from the code and **not** a
defect. Sections 2–7 remain unwalked.

**1 — There is no theme control, and that is why light mode escaped four sittings. Fixed.**
`app.dart` handed `MaterialApp` a `theme:` and a `darkTheme:` and no `themeMode:`, so brightness
belonged to Windows. Seeing the light palette meant changing an OS-wide personalisation setting and
changing it back — a cost paid on the *reader's whole desktop*, for a look at one app. That is a
sufficient explanation for four misses, and it makes this a **drive-instrument defect** rather than
a preference: the sheet's own section 1 was unaffordable.

`AppThemeMode` follows `AppLanguage` line for line — named values, `fromStored` by name so
reordering cannot rewrite an existing install, `system` as the default so an install that never
opens Settings is unchanged. It is the third card under *Settings → Display*, and last of the
three: language and date format can each make the screen unreadable, and this one cannot.

*Recorded and not done:* `app.dart` handing the mode over is still checked only by driving. The new
tests copy those two lines rather than mounting `FlowMapApp`, which needs the router, which needs
the database — the same named gap `language_test.dart` carries for `locale:`.

**2 — The Capacity Type `ⓘ` cannot be tapped. Fixed, and it was Flutter rather than us.** The icon
sat inside `InputDecoration.label`, and `_RenderDecoration.hitTestChildren` hit-tests the label
through its **paint offset only** — its own comment says *"the label must be handled specially
since we've transformed it"* and then handles it like every other child, ignoring the scale a
floating label is painted through. So the icon is painted in one place and hit in another. This
dropdown always floats its label, because it always has a value.

It was **the only site of its kind in the app**: eighteen other fields hang their help on
`suffixIcon`, which is hit-tested normally. It now uses `namedHelp` above the control — the pattern
the icon grid two lines below it already follows.

**3 — Parallel capacity is already in the denominator. Not a defect, and nothing to change.** The
premise — *"the capacity hours will be the same"* — is the part that is wrong. A workcenter carries
`parallel_capacity` (*"Orders at once"* in the editor), it reaches the engine as `units`, and
`engine.dart:1290` and `:1310` multiply by it in **both** capacity paths, the run total and the
monthly rows: `openTimeBetween(...) * units`. A workcenter set to 2 has **twice the capacity hours**,
so an unchanged demand halves its occupation. The field's help already says so — *"twice the
capacity everywhere it is measured"* — and one workcenter in the live plant, **TTAT**, is already set
to 2.

*Worth confirming on screen anyway*, because it has never been driven: TTAT's occupation should
read half what its raw demand-over-one-machine would. Added as the second half of check 32.

### Section 2, same sitting — the first clean pass this sheet has had

Reported: **"looks good"**.

**Five of the nine are answered, and four are not.** The distinction is what a look can settle.
Checks **5, 6, 7, 9 and 10 pass** — the chart and grid scroll as one with each bar over its own
column, the dotted gridlines read as dotted against the capacity line, the hours axis stays pinned
outside the horizontal scroll, the row height moves between 60 pt and 40 pt with the qualifier, and
all three units stay legible in a 72 px column. That is the first section of any drive sheet in this
repo to come back confirmed rather than adjusted, and it covers the surface rebuilt three times in
eight days.

**Checks 8, 11, 12 and 13 remain open**, because each needs a hand on a control or an opinion rather
than a glance: 11 needs an order-level filter applied and a dimmed remainder read off a cell; 12
needs the thresholds changed and an invalid pair typed; 13 needs *Per line* selected **and the view
filtered to one line**; and 8 asked a question rather than for a verdict.

Two of the four were narrowed from the code and the database while the sitting was open:

**12 — the refusal exists and is silent.** `project_settings_screen.dart:413` computes `crosses`,
and a crossing or non-positive value takes the branch that resets `_controller.text` to the stored
value and returns. So a red below the amber **is** refused rather than stored, which is what the
check demanded. Nothing says so on screen — the field snaps back with no message. Whether that reads
as *refused* or as *my typing was ignored* is the half of the check still owed, and only a person at
the glass can answer it.

**13 — the worry cannot occur on the view the check describes, and now has a precise condition.**
Computed for run `65541123` over its own fifteen months, no line row equals the TOTAL:

| row | demand | capacity | |
|---|---|---|---|
| Fluxo 11C | 39,262 h | 74,120 h | 53.0 % |
| Fluxo 11D | 41,214 h | 66,708 h | 61.8 % |
| Fluxo 11B | 33,757 h | 59,296 h | 56.9 % |
| Fluxo 17A | 19,569 h | 37,060 h | 52.8 % |
| Fluxo 10B | 17,149 h | 37,060 h | 46.3 % |
| Fluxo 10C | 15,365 h | 29,648 h | 51.8 % |
| *(no line)* | 5,193 h | 7,412 h | 70.1 % |
| **TOTAL** | **82,072 h** | **140,828 h** | **58.3 %** |

The rows sum to **171,509 h against a TOTAL of 82,072 h** — 36 line-memberships across 18 workcenters,
so most workcenters sit on two lines. That is the overlap the map has been describing in words, in
numbers, for the first time.

**And it says when the collision happens.** A line's row equals the TOTAL exactly when the view is
*already filtered to that line's own workcenters*, so the line covers everything in view and its row is
arithmetically the total. The map recorded it seen on "Fluxo 11B, a 9-workcenter view" — a filtered
view, which is the condition. On the unfiltered 18-workcenter grid it is impossible. **Check 13 as
written would never have reproduced it**, because it says to switch to *Per line* and says nothing
about filtering first. The check is corrected above.

### Section 2's remaining four, same sitting

Reported: **"11, 12 - ok"**, and on 13: *"I filtered only célula 11B but still shows the other
lines."* Plus, unprompted: *"the workcenter filter should be workcenter right? Add this taxonomy
standardization to what we need to do."*

**11 and 12 pass.** The order-level filter dims the remainder and leaves the % label counting the
grey, so an overload cannot be filtered away; the threshold pair moves the bands and refuses a red
below the amber. Section 2 is closed except for check 8's question.

**13 is not a defect — the grid was right and the check named the wrong filter.** *Célula 11B*
is a **study**; *Fluxo 11B* is a **line**; they are different workcenter sets, 9 against 7. The study's
nine workcenters — BAN11, CEU26, CEU27, CLAD04, CLAD07, CLAD08, Coating, END, TTAT — belong to
**six** lines between them, so six line rows is the correct answer and is the overlap this check
exists to show. The TOTAL-equals-one-row collision needs a *line* filter, which is the only thing
that collapses the view to exactly one line's workcenters.

**And the reason the check named the wrong filter is the finding that came with it.** The instruction
was written from this project's own notes, and those notes had already merged the two: the v2.0 map
records the collision as *"seen on the real database (Fluxo 11B, a 9-workcenter view)"* — a line's
name against a study's workcenter count. The record was wrong before the drive was.

**The taxonomy finding, counted.** In `app_en.arb`, **workcenter** appears 42 times across 36 strings
and **workcenter** 35 times across 34, for one concept; *machine* adds 6 more. Two keys are named for
one and worded as the other — `workcenterLinesHelp` and `workcenterParallelCapacityHelp` both say
*workcenter*. The grouping control offers *Per workcenter* while the help beside it says *workcenter*.
Separately, studies are named `Célula 11B` after a **cell** (`Célula 11`) while being scoped
to a **line** (`Fluxo 11B`). Three ARB files, 551 keys each.

It is a ticket rather than a fix, because *which* word wins is a decision with a schema on one side
of it: [What is each thing called, and does one name win?](https://github.com/matheus-sancha/FlowMap/issues/29)

### Section 3, same sitting — passed, with one question left standing

Reported: **"14, 15, 16 - Ok"**.

**All three pass.** The slicer's 36 stops drag from both ends and the two date fields beside the
track work with it; the dropdown re-columns the Occupation matrices across all four grains and
leaves the Delivery Float alone; and the coarsening behaves as computed — Year reads clean, Month
brings **142.8 %** back, and dragging the slicer to the run's own fifteen months takes the TOTAL from
**24.8 %** to **58.3 %**. A surface that landed the day before and had never been driven came back
confirmed on every check.

**What "Ok" settles is the mechanism, not the judgement.** Check 16 asked a second thing — *whether
the dropdown makes the trade visible enough to be safe* — and that is a design decision rather than
a check. It is now one of **two open questions this section and the last one raised**, both about
what the Occupation surface tells a reader who is not the author:

1. **Does a band that never changes width earn a third of the legend?** (check 8) Rework is 3.5 % in
   eleven of the fifteen months and 3.4–3.6 % in all of them. Changeover, drawn the same way, swings
   0.0–3.5 % and does carry information.
2. **Is the coarsening trade visible enough to be safe?** (check 16) Thirty-nine overloaded
   workcenter-months at Month grain become **zero** at Year, where the worst cell on the grid is 96.1 %.
   Month is the default for that reason, and nothing on screen says why it is.

Both are sharpened by numbers now rather than by suspicion, and both are about the same thing: a
grid that is honest at one setting and reassuring at another, with no caption to say which you are
looking at.

## What this settled

**Closed 2026-09-08 at the field's word.** Two sittings, thirty-five checks, **sections 1, 2 and 3
answered and sections 4, 5, 6 and 7 never walked**.

### Answered

| section | verdict |
|---|---|
| **1 — Light mode** | Two defects, both fixed. The theme picker and the unreachable `ⓘ`. |
| **2 — Occupation** | **All nine checks pass**, one design question left standing (check 8). |
| **3 — Slicer and granularity** | **All three checks pass**, one design question left standing (check 16). |

**This is the first drive in the repo's history to return confirmations rather than adjustments.**
`DRIVE-2026-08-31.md` asked 23 questions and came back with four adjustments and no answers; this
sheet's first sitting produced five findings and walked none of it. The second sitting walked three
sections and eleven of the twelve checks in sections 2 and 3 passed on first contact — a surface
rebuilt three times in eight days, and a slicer that had landed the previous day.

### Three defects found, all fixed the same day

1. **No theme control** (`7314adf`). The reason light mode escaped four sittings: brightness belonged
   to Windows, so looking at the light palette cost an OS-wide setting changed and changed back.
   *Settings → Display → Appearance* now offers Follow the system · Light · Dark.
2. **The Capacity Type `ⓘ` could not be tapped** (`7314adf`). Flutter hit-tests a floating
   `InputDecoration.label` through its paint offset alone, ignoring the scale it is painted through,
   so the icon was painted in one place and hit in another. It was the only site of its kind.
3. **The sheet's own predicted numbers were a different run's** (`a779923`). 28.5 / 68.4 / 148.5
   against this run's 24.8 / 58.3 / 142.8. Stale predictions manufacture false findings, which is
   the one thing a drive sheet must not do.

### Two findings that were not defects, and cost more than the defects did

- **Parallel capacity was already right.** *"The capacity hours will be the same"* was the wrong
  premise: `units` multiplies both capacity paths in `engine.dart`, and TTAT reads **57.1 %** where a
  single machine would read 114.2 %.
- **Check 13 could never have failed, because the record it was written from was already wrong.**
  It asked for a filter on *line Fluxo 11B* (7 workcenters) and the map had recorded the observation as
  *"Fluxo 11B, a 9-workcenter view"* — a line's name against the workcenter count of *study Célula 11B*.
  Both corrected. This produced the sitting's largest finding.

### Two tickets

- [What is each thing called, and does one name win?](https://github.com/matheus-sancha/FlowMap/issues/29)
  — the field's own: *"the workcenter filter should be workcenter right?"* **workcenter** appears 42 times
  and **workcenter** 35 for one concept, across three ARB files, and studies named `Célula 11B`
  after a cell are scoped to a line. It has already corrupted this project's written record twice.
- The **two open design questions** — whether a rework band that never changes width earns a third of
  the legend (check 8), and whether the coarsening trade is visible enough to be safe when 39
  overloaded workcenter-months become zero at Year (check 16). Not yet ticketed.

### Still owed, and honestly so

**Sections 4, 5, 6 and 7 were never walked** — the grids and their edge auto-scroll at 130 rows,
the four study tabs phase 3 never opened, the text sweep after §12.7b, and phase 10's pacing
surface. Sections 4 and 5 have now been owed since 2026-08-31 across three sittings. Nothing in this
file claims anything about them.

<!-- This file stays: sections 4-7 are unanswered. Its answered half has been folded into
     docs/TODO.md phases 3 and 5. -->
